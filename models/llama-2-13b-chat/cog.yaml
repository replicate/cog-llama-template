# Configuration for Cog ⚙️
# Reference: https://github.com/replicate/cog/blob/main/docs/yaml.md

build:
  # set to true if your model requires a GPU
  gpu: true
  cuda: "11.8"

  # python version in the form '3.8' or '3.8.12'
  python_version: "3.11"

  # a list of packages in the format <package-name>==<version>
  python_packages:
    - "numpy==1.24.2"
    - "sentencepiece==0.1.97"
    - "jinja2==3.1.2"
    - "scipy==1.11.1"
    - "asyncio==3.4.3"
    - "safetensors==0.3.1"
    - "ninja==1.11.1"
    - "python-dotenv"
    - "py7zr"
    - "fire"
    - "datasets"
    - "git+https://github.com/huggingface/transformers.git@d2295708a64feff485bb40caf0decf7bae5a05a2"
    - "peft==0.4.0"
    - "tensorizer==1.0.1"
    - "accelerate"
    - "scipy"
    - "bitsandbytes" # force upgrade afterwards?
    - "https://r2.drysys.workers.dev/torch/torch-2.1.0a0+git3af011b-cp311-cp311-linux_x86_64.whl"
  run:
    - curl -o /usr/local/bin/pget -L "https://github.com/replicate/pget/releases/download/v0.0.3/pget" && chmod +x /usr/local/bin/pget
    # - pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu118
    # - pip install git+https://github.com/huggingface/transformers.git@d2295708a64feff485bb40caf0decf7bae5a05a2
    # - pip install -q -U bitsandbytes

# predict.py defines how predictions are run on your model
predict: "predict.py:Predictor"
train: "train.py:train"
